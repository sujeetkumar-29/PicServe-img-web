<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PicServe - User Images</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 10px;
        }
        .user {
            margin-bottom: 20px;
            padding: 15px;
            border: 1px solid #ccc;
            border-radius: 8px;
        }
        h2 {
            margin: 0 0 10px;
        }
        .images {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }
        .images img {
            width: 150px;
            height: 100px;
            object-fit: cover;
            border-radius: 8px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to PicServe!</h1>
        <a href="/images/new">Add an Image Link</a>
        <p>Below are the images uploaded by users:</p>
        
        <% users.forEach(user => { %>
        <div class="user">
            <h2>@<%= user.username %></h2>
            <div class="images">
                <% user.images.slice(0, 4).forEach(image => { %> <!-- Show only the first 4 images -->
                <div>
                    <img src="<%= image.url %>" alt="<%= image.filename %>" />
                    <p><%= image.filename %></p>
                    
                </div>
                <% }) %>
            </div>
            <a href="/images/<%= user.username %>">See All Photos</a>
        </div>
        <% }) %>
    </div>
</body>
</html>
